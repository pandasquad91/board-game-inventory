spring:
  jpa:
    hibernate:
      # create = wipe db each launch
      # update = use existing db
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: "jdbc:postgresql://localhost:5432/board_game_inventory"
    username: pandasquad
    password: pandapassword
  data:
    rest:
      basePath: "/api"